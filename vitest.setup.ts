// Global test setup. DOM-specific matchers (@testing-library/jest-dom) are
// registered per-suite in component tests that run under the jsdom environment.
// Engine and validation suites run under the Node environment and require no setup.
export {};
