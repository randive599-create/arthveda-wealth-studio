# ArthVeda · Private Office — Wealth Projection Studio
#
# This is a fully client-side application. It requires NO backend, NO API keys,
# and NO environment variables to run. All projections are computed locally in
# the browser and scenarios are shared via compact, self-contained URLs.
#
# This file is intentionally empty of secrets and exists only to document that
# no runtime configuration is required for local development or production.
